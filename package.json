{
  "name": "root",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@changesets/cli": "^2.27.5",
    "@vitest/browser": "^1.6.0",
    "@vitest/coverage-istanbul": "^1.6.0",
    "conventional-changelog-cli": "^4.1.0",
    "jsdom": "^24.0.0",
    "picocolors": "^1.0.0",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.1",
    "typedoc": "^0.25.4",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0",
    "webdriverio": "^8.39.0"
  },
  "overrides": {
    "prettier": "^3.2.5"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "ci:test": "npm run ci:test -w @webav/*",
    "publish:all": "npm publish -w @webav/*",
    "publish:all:next": "npm publish -w @webav/* --tag next",
    "build:api": "npm run build:api -w @webav/*",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
    "upver": "pnpm changeset version"
  },
  "workspaces": [
    "packages/*"
  ]
}
